# HR Manager (Django + React)

Aplikacja dyplomowa: panel HR do zarządzania pracownikami, obecnością, urlopami i ocenami.

## 1) Backend (Django)

### Instalacja
```bash
python -m venv .venv
# Windows: .venv\Scripts\activate
# Linux/Mac: source .venv/bin/activate
pip install -r requirements.txt
